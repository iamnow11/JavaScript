// Primitive

// 7 types: String, Number, Boolean, null, undefiend, symbol, BigInt

// Refrfence (Non Primitive)
const score = 100;
const scoreValue = 100.3

const isLoggedIn = false
const outsidetemp = null

const anotherID = Symbol('123')
console.log();